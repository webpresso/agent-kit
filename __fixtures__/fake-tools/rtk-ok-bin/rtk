#!/bin/sh
set -eu

case "${1:-}" in
  --version)
    echo "rtk-fixture 0.37.2"
    exit 0
    ;;
  init)
    mkdir -p .claude/hooks
    cat > .claude/hooks/rtk-rewrite.sh <<'EOF'
#!/bin/sh
set -eu
payload="$(cat)"
case "$payload" in
  *'"command":"git status"'*|*'"command": "git status"'*)
    printf '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"rtk git status"}}\n'
    ;;
  *)
    printf '{}\n'
    ;;
esac
EOF
    chmod +x .claude/hooks/rtk-rewrite.sh
    cat > .claude/settings.json <<'JSON'
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "wp-pretool-guard",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "oh-my-codex/dist/scripts/codex-native-hook.js",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "RTK_TELEMETRY_DISABLED=1 RTK_HOOK_EXCLUDE_COMMANDS=\"\" \"$CLAUDE_PROJECT_DIR/.claude/hooks/rtk-rewrite.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
JSON
    echo "rtk-fixture: init -g --auto-patch ran"
    exit 0
    ;;
  *)
    echo "rtk-fixture: unknown subcommand: ${1:-}" >&2
    exit 64
    ;;
esac
