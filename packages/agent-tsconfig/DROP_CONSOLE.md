# Drop Console Notes

This file is retained as a short note only.

The older contents were a guide tied to specific build-tool assumptions and rollout notes that can drift quickly from the current app and package configuration.

## Current Rule

Use the current Vite, Wrangler, and package build configs as the source of truth for whether and how console statements are handled in production builds.

## Related Files

- `packages/foundation/typescript/`
- `apps/web/platform-web/`
- `apps/web/admin-web/`
- `apps/web/website/`
