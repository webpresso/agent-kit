#!/usr/bin/env node
/**
 * `ak` CLI entrypoint — populated in Phase 2.
 *
 * Will dispatch to subcommands: blueprint, symlink, init, audit, skills, docs.
 */

console.log('ak: scaffolded. Implementation lands in Phase 2.')
process.exit(0)
