import { describe, expect, it } from 'vitest';
describe('agent-kit seeded compact QA failure', () => {
    it('is intentionally wrong', () => {
        expect(1).toBe(2);
    });
});
//# sourceMappingURL=seeded-failing-test.js.map