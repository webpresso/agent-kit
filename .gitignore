# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/
.output/
.vercel/
.netlify/

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo

# Testing
coverage/
.nyc_output/

# E2E Testing
playwright-report/
test-results/
*.auth.json

# Environment files
.env
.env.local
.env.*.local
!.env.example
.dev.vars

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE — JetBrains
.idea/
*.iml
*.iws

# IDE — VS Code (keep settings, ignore personal)
.vscode/*.log

# Misc
*.backup
*.bak
.cache/
.context/
tmp/
.tmp/

# Agent surfaces — regenerated by `ak setup` and `ak symlink sync`.
# Edit the catalog in @webpresso/agent-kit, then run `ak setup --overwrite`.
.agent/
.agents/
.gemini/
.cursor/
.windsurf/
.omx/
.opencode/
# .claude/ is partially tracked:
#   settings.json, hooks/, rules/ — committed so worktrees inherit them
#   worktrees/ and skills/ — gitignored (runtime / user-global)
.claude/worktrees/
.claude/skills/
.claude/scheduled_tasks.lock
.codex/*
!.codex/hooks.json
!.codex/agents/
!.codex/agents/**
!.codex/skills/
!.codex/skills/**
.codex/skills/.system/**
!.codex/prompts/
!.codex/prompts/**

# ak setup safe-default scaffolder outputs — never commit
*.new

# tshy build cache — regenerated on every build
.tshy/

# Generated reports (mutation, coverage summaries, audit dumps)
reports/

# Stryker scratch — written next to stryker.config when running locally
stryker-setup-*.js

# Test sandbox: __fixtures__/fake-home/ exposes a fake $HOME for test runs.
# Anything under .vite-plus/ is a runtime install with absolute symlinks
# that won't transfer between machines — keep tracked fixtures elsewhere.
__fixtures__/fake-home/.vite-plus/

# === agent-kit: generated (do not edit between these markers)
.claude/skills/
.codex/skills/
.cursor/rules/
.windsurf/rules/
.gemini/commands/
.opencode/agents/
.opencode/commands/
.agents/skills/
.agent/.merged.provenance.json
.agent/.compile-manifest.json
.agent/.rotation-log.jsonl
.agent/.blueprints.db
.agent/.blueprints.lock
.agent/.blueprints.snapshot.sql
.agent/.tail-hint-history.jsonl
# === agent-kit: generated end
