brew "node@24"

# Homebrew Bundle does not support exact patch-version locks for core formulae.
# Install the supported Node 24 formula here, and use the repo-local
# .node-version / .nvmrc pins for the exact 24.16.0 runtime contract.
