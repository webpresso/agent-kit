#!/usr/bin/env sh
set -eu

# >>> managed by webpresso (husky-commit-msg)
# Webpresso does not enforce Lore trailers here; squash commits are the durable review boundary.
# <<< managed by webpresso (husky-commit-msg)

# >>> user-owned (husky-commit-msg)
# Add repo-local commit-msg checks here. This block is preserved by `wp setup`.
# <<< user-owned (husky-commit-msg)
