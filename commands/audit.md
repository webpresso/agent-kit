---
description: Run a webpresso audit (tph | catalog-drift | package-surface | docs-frontmatter | blueprint-lifecycle | bundle-budget | commit-message | tech-debt)
---
Use the `mcp__webpresso__wp_audit` tool to run an audit. Pass `kind` as one of `tph`, `catalog-drift`, `package-surface`, `docs-frontmatter`, `blueprint-lifecycle`, `bundle-budget`, `commit-message`, or `tech-debt` based on what the user asks for.
