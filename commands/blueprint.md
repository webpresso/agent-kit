---
description: Manage blueprints (new | audit | list) via agent-kit MCP
---
Use the `mcp__agent-kit__ak_blueprint` tool to manage blueprints. Pass `action` as one of `new`, `audit`, or `list`, plus action-specific arguments (e.g. `goal` and `complexity` for `new`, `path` for `audit`).
