#!/usr/bin/env sh
set -eu
# Use the direct binary path — npx exits 1 in some closed-stdin git-commit
# subshell contexts (npm/11.x quirk), causing silent pre-commit failures.
"$(git rev-parse --show-toplevel)/node_modules/.bin/ak" audit guardrails
