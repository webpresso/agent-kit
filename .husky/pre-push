#!/usr/bin/env sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"

"$ROOT/bin/wp" audit guardrails --affected --branch
"$ROOT/bin/wp" typecheck --affected --branch
"$ROOT/bin/wp" test --affected --branch
"$ROOT/bin/wp" audit blueprint-pr-coverage --base origin/main
