---
"@webpresso/agent-kit": patch
---

Keep hook vendor drift audit output machine-readable by default and stabilize generated hook smoke coverage so every managed hook launcher is exercised deterministically without corrupting JSON or shared smoke logs.
