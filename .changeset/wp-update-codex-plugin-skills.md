---
'@webpresso/agent-kit': patch
---

Refresh the installed Codex plugin cache after `wp update` updates the global agent-kit package so bundled skills such as `agent-kit:verify` and `agent-kit:tph` are available in new Codex sessions.
