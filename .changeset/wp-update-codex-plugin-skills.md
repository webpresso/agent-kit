---
'@webpresso/agent-kit': patch
---

Refresh the installed Claude Code and Codex plugin caches after `wp update` updates the global agent-kit package so bundled skills such as `agent-kit:verify` and `agent-kit:tph` are available in new agent sessions.
