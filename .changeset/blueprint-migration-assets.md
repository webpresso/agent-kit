---
"@webpresso/agent-kit": patch
---

Ship blueprint SQLite migration SQL assets in the published package so `wp audit blueprint-lifecycle` can initialize ephemeral projection databases for npm consumers.
