---
"@webpresso/agent-kit": patch
---

Add the `wp_audits` MCP tool for deterministic batch audit execution with shared `wp_audit` dispatch, aggregate partial-failure reporting, and routing guidance for multi-audit requests.
