---
'@webpresso/agent-kit': patch
---

Add a shared `architecture-drift` audit for architecture contracts, blueprint linkage, and before/after architecture enforcement, and expose it through the CLI + MCP audit surfaces.
