---
"@webpresso/agent-kit": patch
"@webpresso/agent-vitest": patch
---

Inline `@webpresso/agent-kit` in Node Vitest config so the `bun:sqlite` alias is applied when agent-kit is imported from `node_modules`.
