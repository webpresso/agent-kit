---
"@webpresso/agent-kit": patch
---

Add measured session-memory benchmark recall scoring, report recall reason/error details, allow local single-workspace smoke runs to use an already logged-in Claude CLI via `BENCH_AUTH_MODE=claude-login`, have setup-generated gitignore rules cover Stryker mutation artifacts, and keep toolchain audits out of generated Windsurf/Gemini surfaces.
