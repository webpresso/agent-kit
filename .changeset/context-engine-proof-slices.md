---
"@webpresso/agent-kit": patch
---

Add measured session-memory benchmark recall scoring, report recall reason/error details, and allow local single-workspace smoke runs to use an already logged-in Claude CLI via `BENCH_AUTH_MODE=claude-login`.
