---
"@webpresso/agent-config": minor
---

Add `createNodeProjects` extension seams for extra aliases, inline deps, and setup files, and publish a new `@webpresso/agent-config/vitest/source-conditions` helper for `@webpresso/source` resolver wiring.
