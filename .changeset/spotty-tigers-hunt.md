---
'@webpresso/agent-kit': patch
---

Migrate agent-kit releases to Changesets Action with trusted-publishing support, and make `wp ci act` able to run no-secret local CI rehearsals without requiring an external `with-secrets` wrapper.
