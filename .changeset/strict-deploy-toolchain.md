---
"@webpresso/agent-kit": minor
---

Add the generic `wp deploy` adapter contract plus a toolchain-isolation audit so consumer repos can delegate TypeScript/Vite/Vitest/Stryker/Playwright/Wrangler/Oxlint/tsx execution to agent-kit-owned wrappers instead of direct dependencies or scripts.
