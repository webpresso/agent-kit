---
"@webpresso/agent-kit": patch
---

Future-proof root package publishing by explicitly preparing the packed manifest during release publish, preventing local dependency protocols from reaching published metadata and preserving runtime optional dependency metadata.
