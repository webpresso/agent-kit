---
"@webpresso/agent-kit": patch
---

Fix `wp setup --dry-run` flag handling, avoid repeated Codex hook trust sync during setup, and collapse gstack Codex/team setup into one upstream setup invocation when Codex is available.
