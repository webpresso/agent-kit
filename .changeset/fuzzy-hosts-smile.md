---
"@webpresso/agent-kit": patch
---

Fix setup host visibility for Codex, Claude Code, and OpenCode, remove unsupported `.codex/agents` projections, and persist required core skill checks for `verify` and `plan-refine`.
