---
"webpresso": minor
---

Hardcut the package, plugin, MCP, workflow, and documentation identity to the canonical `webpresso` package with subpath exports and no legacy helper-package compatibility layer.
