---
"@webpresso/agent-kit": patch
---

Harden final MCP productivity guardrails: sanitize parsed read-only details, keep release readiness read-only, isolate Claude-login benchmarks from ambient API keys, require explicit Claude CLI login status, and restrict worktree removals/pruning to registered managed entries.
