---
"@webpresso/agent-kit": patch
---

Fix release finalization after custom Changesets publish so successful npm
publishes always create the release tag, compatibility branch, GitHub Release,
and runtime binary assets.
