---
'@webpresso/agent-kit': patch
---

Fix package-private import aliases for blueprint execution types and cross-repo audits so published CLI and dist modules resolve without consumer-side node_modules patches.
