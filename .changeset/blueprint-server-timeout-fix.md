---
'@webpresso/agent-kit': patch
---

Avoid unnecessary blueprint projection re-ingest during tool registration, allow shared project resolver injection for blueprint server registration, and split heavy blueprint server tests so the MCP suite stays within verification budgets.
